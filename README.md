# Calculus BC Web Scraper

## Instructions to run:

1. Create a file called `.env` in the root folder with the following:

```
SCHOOLOGY_EMAIL=<your-email>
SCHOOLOGY_PASSWORD=<your-password>
```

2. Run the following commands:

```
npm install
npm run build
npm start
```
